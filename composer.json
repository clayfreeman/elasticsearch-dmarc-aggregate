{
    "name": "clayfreeman/elasticsearch-dmarc-aggregate",
    "description": "Parses DMARC aggregate report attachments from an IMAP connection, then inserts the data into Elastic Search.",
    "version": "1.0.0",
    "type": "project",
    "require": {
        "elasticsearch/elasticsearch": "^6.0",
        "ext-curl": "*",
        "ext-imap": "*",
        "ext-zip": "*",
        "ext-zlib": "*",
        "php": ">=7.2",
        "wapmorgan/unified-archive": "^0.0.10",
        "zbateson/mail-mime-parser": "^0.4.8"
    },
    "license": "LGPL-3.0-or-later",
    "authors": [
        {
            "name": "Clay Freeman",
            "email": "git@clayfreeman.com"
        }
    ],
    "minimum-stability": "stable"
}
